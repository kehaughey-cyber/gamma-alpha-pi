# website — the forward-facing door

The public site is the first thing a man meets before any brother does. This lane holds
what the site must *say* and *do* — the spec, the copy, the flow from stranger to
intake — not the site's code (that is the collaborator's build) and not the marketing
funnel machinery (that stays in the quarry until a specific piece is needed). This lane
serves **Step 4: cross the minimum public-launch threshold.**

## Current state
The storefront is **already live** on Wix at `gammaalphapi.com`:
Home / About / Join Us / Books, Coin and Merch / Contact / Program List, plus a Discord
and a PayPal donation. The Wix editor backend is behind Kevin's login — a **manual seam**;
Kevin drives it or grants access when we work it.

So the site exists. The open question is not "build a site" — it is **does the live site
say the true thing and route a man to the walk.** That gap is what this lane closes.

GAP also lives on YouTube, TikTok, Facebook, and Discord. All live public surfaces are
registered in `channels.md` (URLs, funnel role, audit status). The video channels are the
rescue voice at the top of the funnel; the site is the crossing; Discord is the community
container. `channels.md` is the checklist for the Step-4 copy/content audit.

## What is here now
- **`site-plan.md`** — the complete page-by-page rebuild plan for GammaAlphaPi.com: purpose,
  structure, and exact copy for all six pages, plus colors, typography, and an implementation
  priority. The canonical site spec and copy of record.
- **`copy-audit.md`** — the site plan checked against the live site and the repo canon:
  strong alignment, one tension to settle (the life-arc framing on the About page), and the
  live-page verification that needs the Wix login.
- **`channels.md`** — the live public surfaces (site + YouTube/TikTok/Facebook/Discord).
- **`wix-checklist.md`** — WP1 implementation tracker: what's live (Join Us form reworded +
  required, "You knocked." autoresponse ACTIVE) and the canvas parts still to do by hand.

## Still to curate when the need is live
- **Program of work** — `WBS for creating a global fraternity.docx` (broader than the site).
- **Launch copy** — mine specific pieces from `Marketing/` and `GAP/GAP_Seek_Ye_First_Master.docx`
  (the sermon / TikTok master content) as the site and campaigns need them. Do **not** import
  the whole marketing folder; it is archive.
- **The intake handoff** — where the public site ends and `operations/` intake begins. The
  `site-plan.md` Join Us page + autoresponse is exactly this seam; keep it aligned with the
  `operations/` intake as that stands up.

## Non-negotiables
- **House voice on the public page too** (`system/voice.md`): orient, do not flatter or
  sell. A funnel that flatters betrays the thing before a man is even in.
- The site's job is to get the right man to **start** (the first of the four problems),
  and to be honest enough that the wrong man self-selects out.

## Build order
Reconcile what the live site says against the current corpus and system truth first
(copy audit). A rebuild, if it happens, is downstream of knowing what it must say.
